import React from "react";

export default function FeedbackTile() {
  //generates a new grid item with if the item was correct, higher or lower than the correct value
  return <div>FeedbackTile</div>;
}
